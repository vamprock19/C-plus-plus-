#include <iostream>;
using namespace std;


/*
*  RELATIONAL OPERATORS
   > grater than
   >= greater than or equal to
   < less than
   <= less than or equal to 
   == equal-to (equality)
   != not-equal-to (equality)
*/
int main()
{
	int num1 = 20;
	int num2 = 25 ;

	cout << boolalpha << endl;
	cout << (num1 <= num2) << endl;






	/*
	
	    cout << boolalpha << endl;
		int a = 15;
		int b = 20;
		bool AreEqualTo = a == b;
		int age = 16;
	

		cout << (a < b) << endl;
		cout << AreEqualTo << endl;
		cout << (age >= 21) << endl;
	
    */
	
	return 0;
}